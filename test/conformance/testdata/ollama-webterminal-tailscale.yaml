apiVersion: hermes.agent/v1
kind: HermesInstance
metadata:
  name: conformance-sidecars
spec:
  image:
    repository: ghcr.io/paperclipinc/hermes-agent
    tag: v2026.5.29.2
  storage:
    persistence:
      enabled: true
      size: 5Gi
  sidecars:
    - name: ollama
      image: ollama/ollama:0.5.7
      ports:
        - containerPort: 11434
          name: ollama
      resources:
        requests:
          cpu: 500m
          memory: 2Gi
    - name: web-terminal
      image: ghcr.io/paperclipinc/hermes-web-terminal:v0.1.0
      ports:
        - containerPort: 7681
          name: web-terminal
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
    - name: tailscale
      image: tailscale/tailscale:v1.78.1
      env:
        - name: TS_AUTHKEY
          valueFrom:
            secretKeyRef:
              name: tailscale-auth
              key: authkey
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
